# Slim runtime deps for the deployed container. No shap/jupyter/optuna/streamlit/torch —
# SHAP values come from XGBoost's native pred_contribs, so the heavy shap→numba→llvmlite
# stack is dropped (smaller image, lower memory, faster cold start on Render's free tier).
fastapi>=0.110
uvicorn[standard]>=0.27
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
pyarrow>=15.0   # the saved artifacts use pandas pyarrow-backed strings
